# Copy this file to .env and adjust values as needed.

# Port nginx is exposed on the host (container always listens on 80 internally)
FRONTEND_PORT=5173

# Base URL the React app uses to call the backend API.
# This gets baked into the static build at `docker compose build` time.
VITE_API_URL=http://localhost:8001
